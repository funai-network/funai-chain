# Pin lax — this is research-grade. Phase 1 will tighten if needed.
# torch must be a CUDA-enabled build for Phases 1 and 2; install via:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
# adjust cu121 to match the PoC host's CUDA version.
torch>=2.1,<3
transformers>=4.40,<5
numpy>=1.24,<2
pytest>=7
# ChaCha20 for Phase 1b deterministic sampling (V5.2 §9.3).
cryptography>=42
# Optional: required only when V6_MODEL points at an AWQ-quantized
# checkpoint (test_phase1_moe.py with Mixtral-8x7B-AWQ etc.). Leave
# uninstalled when only running bf16 dense / MoE — autoawq pulls in
# CUDA-specific kernels that are not needed for the default path.
# Install separately:
#   pip install autoawq
